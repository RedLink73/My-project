using System;
using UnityEngine;


public class SceneChange : MonoBehaviour
{
    // We can set up a trigger collider in the scene and attach this script to it. When the player enters the trigger, we can load the next scene.
    
    void Start()
    {
        
    }

    void Update()
    {
    }
}